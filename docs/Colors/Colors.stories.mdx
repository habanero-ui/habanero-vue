import Box from '../../src/components/Box/index'
import tailwindConfig from '../../tailwind.config'
import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Styles/Colors" />

export const groups = [
  {
    name: 'Primary',
    colors: ['gold', 'beige', 'dark-beige'],
    description: 'This is a description about these colors.',
  },
  {
    name: 'Secondary',
    colors: ['blue', 'green', 'orange', 'pink', 'purple'],
    description: 'This is a description about these colors.',
  },
  {
    name: 'Supportive',
    colors: [
      'border',
      'error',
      'info',
      'offwhite',
      'subtle',
      'success',
      'warning',
    ],
    description: 'This is a description about these colors.',
  },
  {
    name: 'Surface',
    colors: ['white', 'heavy-black', 'black'],
    description: 'This is a description about these colors.',
  },
]

# Colors

Insert info on colors

<div>
  {groups.map((group) => (
    <div key={group.name} style={{ marginBottom: 40 }}>
      <h2 style={{ marginBottom: 16 }}>{group.name}</h2>
      <p>{group.description}</p>
      {group.colors.map((color) => (
        <div
          key={color}
          style={{
            display: 'flex',
            flexDirection: 'row',
            alignItems: 'center',
            justifyContent: 'space-between',
            height: '3rem',
            padding: '1rem',
            backgroundColor: tailwindConfig.theme.extend.colors[color],
            textTransform: 'capitalize',
            cursor: 'pointer',
          }}
        >
          <div 
            style={{
              fontSize: '0.75rem',
              lineHeight: '0.875rem',
            }}
          >
            {color.replace('-', ' ')}
          </div>
          <div 
            style={{
              fontSize: '0.75rem',
              lineHeight: '0.875rem',
            }}
          >
            {tailwindConfig.theme.extend.colors[color]}
          </div>
        </div>
      ))}
    </div>
  ))}
</div>
